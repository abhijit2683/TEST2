name: Upload to S3

on: [pull_request]

jobs:
  upload:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@master
      - uses: abhijit2683/TEST2@master
        with:
          aws_key_id: ${{ secrets.AWS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_KEY}}
          aws_bucket: myabhi-bucket
          source_dir: 'sub'
